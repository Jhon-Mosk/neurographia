# Neurographia — Терминальный тренажёр английского языка

🧠 Интерактивный CLI-тренажёр для изучения английских фраз через метод интервального повторения. Вдохновлён нейрографией — методом визуализации и закрепления знаний.

---

## 📋 Описание

Neurographia — это минималистичное консольное приложение для эффективного изучения английских фраз. Проект использует системный подход к обучению:

- **Приоритезация по сложности** — фразы показываются от простого (A1) к сложному (C2)
- **Активное вспоминание** — сначала русский, затем английский, затем подтверждение изучения
- **Прогрессивное повторение** — изученные фразы больше не показываются
- **Статистика в реальном времени** — отслеживание прогресса по уровням

---

## ✨ Возможности

- 📚 Работа с фразами 6 уровней сложности (A1–C2)
- 🗄️ Локальное хранение данных в SQLite
- 📊 Детальная статистика по уровням с процентами выполнения
- 🔄 Идемпотентная инициализация — безопасный повторный запуск
- ⌨️ Простое управление через клавиатуру (Enter, y/n, q для выхода)
- 📁 Импорт фраз из JSON-файла
- 🚀 Минимальные зависимости — только Node.js и SQLite

---

## 🛠 Требования

- **Node.js** >= 18.x
- **npm** или **yarn**
- Операционная система: Linux, macOS, Windows (WSL)

---

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd neurographia
```

### 2. Установка зависимостей

```bash
npm ci --omit=dev
```

Установятся следующие пакеты:
- `better-sqlite3` — работа с базой данных

---

## 🚀 Быстрый старт

### 1. Инициализация базы данных

```bash
npm run init-db
```

Эта команда:
- Создаст папку `db/` и файл базы данных `phrases.db`
- Создаст таблицу фраз и необходимые индексы
- Импортирует фразы из `data/phrases.json`
- Покажет статистику по уровням

> Если файл `data/phrases.json` не существует, будет создан пример с 10 фразами разных уровней.

### 2. Запуск тренажёра

```bash
npm start
```

---

## 📖 Использование

### Основной цикл обучения

1. **Запустите приложение**: `npm start`
2. **Видите фразу на русском** — попробуйте вспомнить перевод
3. **Нажмите Enter** — откроется английский вариант
4. **Ответьте на вопрос "Изучено?"**:
   - `y` или `yes` — фраза отмечена как изученная (больше не покажется)
   - `n` или `no` — фраза останется в очереди
   - `q` — выход из сессии
5. **Повторяйте** — следующая фраза автоматически появится на экране

### Управление сессией

| Клавиша     | Действие                  |
| ----------- | ------------------------- |
| `Enter`     | Показать перевод          |
| `y` / `yes` | Отметить как изученное    |
| `n` / `no`  | Отложить на потом         |
| `q`         | Выйти из сессии           |
| `Ctrl+C`    | Принудительное завершение |

---

## 📁 Структура проекта

```
neurographia/
├── db/                     # База данных SQLite
│   └── phrases.db          # Файл БД (игнорируется в .gitignore)
├── data/                   # Данные для импорта
│   └── phrases.json        # Файл с фразами
├── scripts/                # Вспомогательные скрипты
│   └── init-db.js          # Инициализация БД
├── src/                    # Исходный код
│   ├── index.js            # Точка входа
│   ├── db.js               # Работа с базой данных
│   └── cli.js              # Терминальный интерфейс
├── .gitignore              # Игнорируемые файлы
├── package.json            # Зависимости и скрипты
└── README.md               # Эта документация
```

---

## 📝 Формат данных

Фразы импортируются из файла `data/phrases.json`. Формат:

```json
[
  {
    "ru": "Привет",
    "en": "Hello",
    "level": "A1"
  },
  {
    "ru": "Как дела?",
    "en": "How are you?",
    "level": "A1"
  },
  {
    "ru": "Я не понимаю",
    "en": "I don't understand",
    "level": "A2"
  },
  {
    "ru": "Это зависит от обстоятельств",
    "en": "It depends on the circumstances",
    "level": "B2"
  }
]
```

### Поля:

| Поле    | Тип    | Обязательное | Описание                                              |
| ------- | ------ | ------------ | ----------------------------------------------------- |
| `ru`    | string | ✅            | Фраза на русском языке                                |
| `en`    | string | ✅            | Фраза на английском языке                             |
| `level` | string | ✅            | Уровень сложности: `A1`, `A2`, `B1`, `B2`, `C1`, `C2` |

### Добавление новых фраз:

1. Откройте `data/phrases.json`
2. Добавьте новые объекты в массив
3. Запустите импорт: `npm run init-db`

> **Важно:** При повторном запуске `init-db` дубликаты автоматически пропускаются.

---

## 📊 Статистика

После каждой сессии отображается подробная статистика:

```
📊 Статистика сессии:
   Всего фраз: 15
   Изучено: 10
   Отложено: 5
   Время сессии: 120 сек

📈 Общая статистика по уровням:
┌──────────┬──────────┬──────────┬──────────┐
│ Уровень  │ Всего    │ Изучено  │ Осталось │
├──────────┼──────────┼──────────┼──────────┤
│ A1       │     50   │     45   │      5   │ (90%)
│ A2       │     30   │     15   │     15   │ (50%)
│ B1       │     20   │      5   │     15   │ (25%)
│ B2       │     15   │      0   │     15   │ (0%)
│ C1       │     10   │      0   │     10   │ (0%)
│ C2       │      5   │      0   │      5   │ (0%)
├──────────┼──────────┼──────────┼──────────┤
│ ИТОГО    │    130   │     65   │     65   │ (50%)
└──────────┴──────────┴──────────┴──────────┘
```

---

## 🔧 Скрипты npm

| Скрипт    | Команда                   | Описание                                    |
| --------- | ------------------------- | ------------------------------------------- |
| `start`   | `node src/index.js`       | Запуск тренажёра                            |
| `init-db` | `node scripts/init-db.js` | Инициализация/обновление БД                 |
| `dev`     | `nodemon src/index.js`    | Запуск с автоперезагрузкой (для разработки) |

---

## 🛠 Разработка

### Архитектура

Проект разделён на три основных модуля:

1. **`src/db.js`** — работа с базой данных SQLite
   - Инициализация подключения
   - Импорт фраз из JSON
   - Выборка следующей фразы (с приоритезацией)
   - Обновление статуса изучения
   - Получение статистики

2. **`src/cli.js`** — терминальный интерфейс
   - Ожидание нажатия клавиш
   - Запрос ответа от пользователя
   - Основной цикл сессии обучения
   - Отображение статистики

3. **`src/index.js`** — точка входа
   - Инициализация приложения
   - Обработка ошибок
   - Обработка сигнала завершения (Ctrl+C)

### База данных

**Таблица `phrases`:**

| Колонка      | Тип      | Описание                    |
| ------------ | -------- | --------------------------- |
| `id`         | INTEGER  | Первичный ключ              |
| `ru`         | TEXT     | Фраза на русском            |
| `en`         | TEXT     | Фраза на английском         |
| `level`      | TEXT     | Уровень сложности (A1–C2)   |
| `completed`  | BOOLEAN  | Статус изучения (0/1)       |
| `created_at` | DATETIME | Время создания              |
| `updated_at` | DATETIME | Время последнего обновления |

**Индексы:**
- `idx_priority` — оптимизация выборки по приоритету (выполнено, уровень, время)

---

## 🐛 Возможные проблемы

### Ошибка: `Cannot read properties of null`

**Причина:** Методы БД вызываются до завершения инициализации.

**Решение:** Убедитесь, что `initDB()` вызывается перед любыми операциями с БД. Все публичные методы в `db.js` уже содержат эту проверку.

### Ошибка: `База данных пуста`

**Причина:** Файл `data/phrases.json` не существует или содержит некорректные данные.

**Решение:**
```bash
# Создайте файл с примером данных
npm run init-db
```

### Фразы не импортируются повторно

**Причина:** Импорт идемпотентен — дубликаты автоматически пропускаются.

**Решение:** Удалите БД и инициализируйте заново (если нужно сбросить прогресс):

```bash
rm -rf db/
npm run init-db
```

---

## 📝 Планы на будущее

- [ ] Режим повторения — показ только изученных фраз для закрепления
- [ ] Экспорт/импорт прогресса — резервное копирование
- [ ] Таймер на вспоминание — автоматический показ перевода через N секунд
- [ ] Режим "тест" — скрыть русский, показывать только английский
- [ ] Настройка через конфиг-файл (`.neurographia.json`)
- [ ] Поддержка нескольких языков (не только русский→английский)

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл `LICENSE` для подробностей.

---

## 👨‍💻 Автор

Разработано с ❤️ для эффективного изучения английского языка.

---

## 🙏 Благодарности

- **SQLite** — надёжное локальное хранилище
- **Node.js** — кроссплатформенная среда выполнения
- **Нейрография** — вдохновение для метода обучения

---

**Удачного изучения! 🚀**
